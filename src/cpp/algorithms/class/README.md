# examples/cpp/algorithms/class

Warning: This C++ version of the algorithm needs spaces rather than commas as delimiters in the `.csv` maps. A simple way to do this:
```bash
sed -i "s/,/ /g" map1.csv
```
